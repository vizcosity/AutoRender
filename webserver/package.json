{
  "name": "autorender-webserver",
  "version": "1.0.0",
  "description": "RESTful API interface for queueing, checking, removing, and downloading job results for videos rendered with AutoRender Core (super dir).",
  "main": "app.js",
  "api-version": "1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aaron Baw",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
